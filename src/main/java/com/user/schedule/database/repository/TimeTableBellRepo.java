package com.user.schedule.database.repository;

import com.user.schedule.database.model.TimeTableBell;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TimeTableBellRepo extends JpaRepository<TimeTableBell,Integer> {
}
